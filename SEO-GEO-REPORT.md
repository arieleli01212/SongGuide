# דוח SEO + GEO — "שיר ברבע שעה"

כתובת: `https://songuide.open-spaces.xyz/`
נבדק: 31.7.2026 · שפה: he-IL בלבד

---

## מה היה חסר (ומה תוקן)

| פריט | לפני | אחרי |
|---|---|---|
| `canonical` | חסר | ✅ |
| `og:url`, `og:image`, `og:site_name` | חסר | ✅ + תמונה ב-`assets/og.png` |
| Twitter card | חסר | ✅ `summary_large_image` |
| `hreflang` | חסר | ✅ `he-IL` + `x-default` |
| JSON-LD structured data | **0 בלוקים** | ✅ `@graph`: HowTo (7 שלבים) + FAQPage (8 שאלות) + Person + WebSite |
| `robots.txt` | לא קיים | ✅ כולל 13 בוטים של AI |
| `sitemap.xml` | לא קיים | ✅ |
| `llms.txt` | לא קיים | ✅ |
| `dir="rtl"` + `lang="he"` | ✅ כבר היה | — |

**התיקון בעל ההשפעה המיידית הגדולה ביותר הוא `og:image`.** ערוץ ההפצה המרכזי של המוצר הוא שיתוף בוואטסאפ ובקבוצות פייסבוק, ועד עכשיו כל שיתוף כזה הופיע בלי תמונת תצוגה. לינק עם תצוגה מקדימה מקבל שיעור הקלקה גבוה משמעותית מלינק עירום.

---

## הערה חשובה על ציפיות מגוגל

צריך לומר את זה במפורש כדי לא לבנות על ציפייה שגויה:

- **גוגל ביטלה את תוצאות ה-Rich Results עבור HowTo** (2023). סכמת ה-HowTo *לא* תייצר תצוגת שלבים מיוחדת בתוצאות החיפוש.
- **FAQ Rich Results הוגבלו על ידי גוגל לאתרי ממשל ובריאות סמכותיים בלבד** (2023). סכמת ה-FAQ *לא* תייצר אקורדיון שאלות בתוצאות של אתר כזה.

אז למה בכל זאת הוספנו אותן? כי הערך שלהן עבר לזירה אחרת: **מנועי חיפוש מבוססי AI** (Perplexity, ChatGPT search, Google AI Overview, Claude) קוראים JSON-LD כדי לחלץ תשובות ולצטט מקורות. שם הסכמה עדיין עובדת, ושם התחרות נמוכה בהרבה. תתייחסו לזה כהשקעה ב-GEO, לא כהשקעה בסניפט של גוגל.

---

## ✅ הדומיין המותאם פתר את בעיית ה-robots.txt

בגרסה קודמת של הדוח הזה הופיעה אזהרה: ב-GitHub Pages תחת `arieleli01212.github.io/SongGuide/`, קובץ `robots.txt` בשורש הריפו **לא נקרא** — סורקים קוראים אותו רק משורש הדומיין, וזה היה שייך לחשבון GitHub כולו.

**המעבר ל-`songuide.open-spaces.xyz` ביטל את הבעיה.** עכשיו שורש הריפו הוא שורש הדומיין, ולכן:

```
https://songuide.open-spaces.xyz/robots.txt   ← נקרא כרגיל ✅
https://songuide.open-spaces.xyz/sitemap.xml  ← מוגש כרגיל ✅
https://songuide.open-spaces.xyz/llms.txt     ← מוגש כרגיל ✅
```

שורת ה-`Sitemap:` בתוך `robots.txt` תקפה, אבל **עדיין כדאי לשלוח את ה-sitemap ידנית** ב-Search Console תחת Sitemaps — זה מזרז אינדוקס ראשוני ונותן דיווח שגיאות.

⚠️ **קובץ `CNAME` חייב להישאר בריפו.** GitHub Pages מאבד את הדומיין המותאם בכל דיפלוי שבו הקובץ חסר, והאתר חוזר לכתובת ה-`github.io`. אם הוגדר דרך ממשק GitHub, הקובץ נוצר אוטומטית — ודא שהוא קומיט בריפו ולא רק בהגדרות.

⚠️ **Search Console — נכס נפרד.** האימות שביצעת הוא עבור `arieleli01212.github.io/SongGuide`. הדומיין החדש הוא **נכס אחר לגמרי** וצריך אימות משלו. הוסף `songuide.open-spaces.xyz` כנכס נוסף (עדיף Domain property דרך DNS) ושלח שם את ה-sitemap.

---

## מפת מילות מפתח בעברית

הערה על מתודולוגיה: **אין כאן נתוני נפח חיפוש אמיתיים.** לקבלת נפחים צריך Google Keyword Planner (חינמי, דורש חשבון Ads) או DataForSEO. הדירוג כאן הוא לפי כוונת חיפוש והערכת תחרות, לא לפי מדידה.

### אשכול 1 — "איך עושים" (הליבה, תחרות בינונית)

```
איך יוצרים שיר עם AI
איך עושים שיר בבינה מלאכותית
יצירת שיר עם בינה מלאכותית
איך כותבים שיר עם ChatGPT
שיר מותאם אישית AI
```

### אשכול 2 — כלי (Suno) — **תרגום הוא מלכודת**

```
Suno בעברית          סונו בעברית          סאנו בעברית
איך משתמשים ב-Suno    Suno חינם            סונו מדריך
```

תעתיק לטיני-לעברית של שמות כלים מפוצל בין **סונו** ו-**סאנו**, ומשתמשים ישראלים מקלידים את שתי הצורות וגם את הצורה הלטינית `Suno`. כרגע העמוד מכיל רק `Suno`. שווה להזכיר את שתי צורות התעתיק פעם אחת בטקסט טבעי בגוף העמוד.

### אשכול 3 — כוונת מתנה (הכי רגשי, הכי ממיר)

```
מתנה מקורית ליום הולדת    שיר במתנה              מתנה מרגשת לאמא
מתנה מקורית לחבר          שיר לחתונה             שיר לגיוס
מתנה ליום נישואין         שיר לריקוד ראשון
```

### אשכול 4 — שאלות בעיה — **כאן אתה מנצח**

```
למה שיר AI נשמע גנרי
איך גורמים ל-AI לשיר נכון בעברית
Suno לא מבטא נכון שמות בעברית
ניקוד ב-Suno
איך מתקנים שורה אחת בשיר ב-Suno
```

אלה שאילתות עם תחרות כמעט אפסית בעברית, וזה בדיוק החומר שמנועי AI מצטטים. **שלב 4 (ניקוד) הוא הנכס האמיתי של האתר** — זו תובנה ספציפית לעברית שכמעט לא קיימת בשום מקום אחר באינטרנט. הוא מטופל כרגע כשלב פנימי במדריך; הוא ראוי לכותרת ולפסקה שעונים על השאלה ישירות.

### מורפולוגיה — למה זה משנה בעברית

השורש "שיר" מייצר שאילתות נפרדות: **ה**שיר, **ב**שיר, **מ**שיר, **ו**שיר, שיר**ים**, שיר**י** (סמיכות: שירי אהבה, שירי יום הולדת).

⚠️ **מלכודת:** `לשיר` היא גם צורת הפועל "to sing" וגם "לשיר" במובן "אל השיר". אל תבנו תוכן סביבה — כוונת החיפוש מעורבת ותקבלו תנועה לא רלוונטית.

---

## מה עוד כדאי לעשות — לפי סדר עדיפויות

### P0 — לפני הדחיפה של ראש השנה (עד 10.9)

1. **להוסיף סקציית שאלות ותשובות גלויה בעמוד.**
   כרגע 8 השאלות קיימות ב-JSON-LD בלבד. תוכן שמופיע בסכמה אבל לא על המסך מפר את ההנחיות של גוגל, ו-AI מצטט טוב יותר טקסט שהוא רואה ב-HTML. שמונה שאלות בסוף העמוד, כל אחת ב-`<h3>` עם פסקה קצרה. **זה הפריט הכי חשוב ברשימה.**
2. **לוודא ש-GitHub Pages פעיל ושהאתר מאונדקס.**
   `site:songuide.open-spaces.xyz` בגוגל. אם ריק — לרשום את הנכס ב-Google Search Console ולשלוח את ה-sitemap.
3. **לפרסם את השינויים ולבדוק את התצוגה בוואטסאפ.**
   שולחים את הלינק לעצמכם ובודקים שה-`og.png` מופיע.

### P1 — בחודש הקרוב

4. **לשקול דומיין `.co.il`.** `songuide.open-spaces.xyz` כבר עדיף בהרבה על `github.io`, אבל שתי חולשות נשארות: הוא תת-דומיין (יורש את הסמכות של `open-spaces.xyz` במקום לבנות סמכות עצמאית), ו-`.xyz` לא נושא אות גיאוגרפי לישראל כמו `.co.il`. לא דחוף — אבל אם המוצר תופס, דומיין ישראלי עצמאי הוא השדרוג הבא.
5. **להוציא את שלב הניקוד לעמוד משלו** עם כותרת בנוסח "למה Suno לא מבטא נכון שמות בעברית — ואיך מתקנים". תחרות אפסית, ערך ציטוט גבוה, וזה מייצר עמוד שני לאתר.
6. **`Person` schema מלא.** כרגע יש `sameAs` ל-GitHub בלבד. הוספת פרופיל לינקדאין מחזקת E-E-A-T, שהוא אות אמיתי בבחירת מקורות ל-AI Overview.

### P2 — אחרי החגים

7. **גרסה אנגלית.** בחרת עברית בלבד לעכשיו, וזה סביר. שים לב לפער: עמודים בעברית טהורה מצוטטים פחות על ידי ChatGPT ו-Claude מעמודים דו-לשוניים. כשיהיה זמן, עמוד `/en/` עם hreflang דו-כיווני מכפיל את משטח הציטוט.
8. **קישורים נכנסים מדומיינים ישראליים.** אזכור אחד בבלוג טכנולוגי ישראלי או בקבוצת AI גדולה שווה יותר מעשרה קישורים זרים.

---

## איך לבדוק שזה עובד

```bash
# אינדוקס בגוגל
site:songuide.open-spaces.xyz

# ולידציה של הסכמה
https://search.google.com/test/rich-results

# בדיקת תצוגה מקדימה בשיתוף
https://developers.facebook.com/tools/debug/
```

**בדיקת GEO ידנית** — לשאול כל שלושה שבועות בצ'אט של ChatGPT, Perplexity ו-Claude:

> "איך יוצרים שיר אישי עם AI בעברית?"
> "למה Suno לא מבטא נכון שמות בעברית?"

השאלה השנייה היא המדד האמיתי. אם האתר מצוטט עליה — ה-GEO עובד.

---

## סיכום בשורה אחת

הטכני תוקן וזה היה חסר לגמרי, אבל הצעד הבא הוא תוכני ולא טכני: **סקציית שאלות גלויה בעמוד, ושלב הניקוד כעמוד עצמאי.** שם נמצאת התחרות הנמוכה ביותר והידע הייחודי שיש לך ואין לאף אחד אחר.
